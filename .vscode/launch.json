{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "mysqld",
      "request": "launch",
      "type": "cppdbg",
      "program": "${workspaceRoot}/build/bin/mysqld",
      "args": ["--defaults-file=tests/my.cnf"],
      "cwd": "${workspaceRoot}",
      "preLaunchTask": "build",
      "MIMode": "lldb"
    }
  ]
}
